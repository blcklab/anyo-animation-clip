export type SceneAction = (value: unknown, context: SceneActionContext) => void | Promise<void>;
export interface SceneActionContext {
    readonly objectId?: string;
    readonly source?: Event;
}
export declare class SceneActionRegistry {
    private readonly actions;
    register(name: string, action: SceneAction): () => void;
    has(name: string): boolean;
    execute(name: string, value: unknown, context?: SceneActionContext): Promise<void>;
    clear(): void;
}
