import type { Disposable } from './Disposable.js';
export interface ResourceScopeOptions {
    label?: string;
    onDispose?: (scope: ResourceScope) => void;
}
export declare class ResourceScope implements Disposable {
    disposed: boolean;
    readonly label?: string;
    private readonly resources;
    private readonly onDispose?;
    constructor(options?: ResourceScopeOptions);
    track<T extends Disposable>(resource: T): T;
    release(resource: Disposable): void;
    has(resource: Disposable): boolean;
    dispose(): void;
    get size(): number;
}
export declare class ResourceManager implements Disposable {
    disposed: boolean;
    private readonly scopes;
    createScope(label?: string): ResourceScope;
    get stats(): {
        scopes: number;
        trackedResources: number;
    };
    dispose(): void;
}
