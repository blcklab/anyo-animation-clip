import { EnvironmentResource, type ToneMapping } from '../environment/index.js';
export interface ProceduralSkyOptions {
    id?: string;
    width?: number;
    height?: number;
    zenithColor?: readonly [number, number, number];
    horizonColor?: readonly [number, number, number];
    groundColor?: readonly [number, number, number];
    sunColor?: readonly [number, number, number];
    sunDirection?: readonly [number, number, number];
    sunAngularRadius?: number;
    sunIntensity?: number;
    haze?: number;
    cloudCoverage?: number;
    cloudDensity?: number;
    cloudScale?: number;
    cloudSeed?: number;
    intensity?: number;
    exposure?: number;
    toneMapping?: ToneMapping;
    label?: string;
}
/** Creates a deterministic HDR equirectangular sky suitable for backgrounds and lightweight IBL. */
export declare function createProceduralSky(options?: ProceduralSkyOptions): EnvironmentResource;
