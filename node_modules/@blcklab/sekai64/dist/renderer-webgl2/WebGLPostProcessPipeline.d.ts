import { type RendererColorGrading, type RendererImageQuality, type RendererPostProcessing } from '../renderer/index.js';
export declare class WebGLPostProcessPipeline {
    private readonly gl;
    private framebuffer?;
    private color?;
    private depth?;
    private vao?;
    private programs;
    private aoA?;
    private aoB?;
    private bloom;
    private lutTexture?;
    private lutSource?;
    private width;
    private height;
    lastPassCount: number;
    constructor(gl: WebGL2RenderingContext);
    get target(): WebGLFramebuffer | null;
    ensure(width: number, height: number): void;
    composite(configuration: RendererPostProcessing, imageQuality: RendererImageQuality, colorGrading: RendererColorGrading, target?: WebGLFramebuffer | null): void;
    dispose(): void;
    private getProgram;
    private ensureLutTexture;
    private releaseTargets;
}
