import { Color, type ColorInput } from '../math/index.js';
import { Material, type MaterialOptions } from './Material.js';
import { Texture } from './Texture.js';
export interface TextureMaterialOptions extends MaterialOptions {
    map: Texture;
    tint?: ColorInput;
    alphaCutoff?: number;
    ownsTexture?: boolean;
}
/** Lightweight unlit material for text, signs, images, and UI surfaces. */
export declare class TextureMaterial extends Material {
    readonly tint: Color;
    map: Texture;
    alphaCutoff: number;
    ownsTexture: boolean;
    constructor(options: TextureMaterialOptions);
    setMap(map: Texture, ownsTexture?: boolean): this;
    setTint(value: ColorInput): this;
    setAlphaCutoff(value: number): this;
    protected release(): void;
}
