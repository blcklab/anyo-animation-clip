import { Material, type MaterialOptions } from './Material.js';
export type UniformValue = number | readonly number[] | Float32Array;
export interface ShaderMaterialOptions extends MaterialOptions {
    wgsl?: {
        vertex: string;
        fragment: string;
    };
    glsl?: {
        vertex: string;
        fragment: string;
    };
    uniforms?: Readonly<Record<string, UniformValue>>;
}
export declare class ShaderMaterial extends Material {
    readonly wgsl?: Readonly<{
        vertex: string;
        fragment: string;
    }>;
    readonly glsl?: Readonly<{
        vertex: string;
        fragment: string;
    }>;
    readonly uniforms: Map<string, UniformValue>;
    constructor(options: ShaderMaterialOptions);
    setUniform(name: string, value: UniformValue): this;
}
export declare function wgsl(strings: TemplateStringsArray, ...values: readonly unknown[]): string;
export declare function glsl(strings: TemplateStringsArray, ...values: readonly unknown[]): string;
