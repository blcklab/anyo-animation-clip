import { ManagedResource } from '../core/index.js';
export type MaterialSide = 'front' | 'back' | 'double';
export interface MaterialOptions {
    label?: string;
    transparent?: boolean;
    /** Backward-compatible alias for side: 'double'. */
    doubleSided?: boolean;
    side?: MaterialSide;
    depthWrite?: boolean;
    /** Declares an unsupported wireframe request so renderers can diagnose it honestly. */
    wireframe?: boolean;
    /** Stable transparent ordering bias; larger values render later. */
    sortBias?: number;
}
export declare abstract class Material extends ManagedResource {
    transparent: boolean;
    side: MaterialSide;
    depthWrite: boolean;
    wireframe: boolean;
    sortBias: number;
    version: number;
    protected constructor(options?: MaterialOptions);
    get doubleSided(): boolean;
    set doubleSided(value: boolean);
    setSide(side: MaterialSide): this;
    setWireframe(wireframe: boolean): this;
    setSortBias(sortBias: number): this;
    markChanged(): void;
    protected release(): void;
}
