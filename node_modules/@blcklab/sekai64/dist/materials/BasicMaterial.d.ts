import { Color, type ColorInput } from '../math/index.js';
import { Material, type MaterialOptions } from './Material.js';
export interface BasicMaterialOptions extends MaterialOptions {
    baseColor?: ColorInput;
}
export declare class BasicMaterial extends Material {
    readonly baseColor: Color;
    constructor(options?: BasicMaterialOptions);
    setBaseColor(value: ColorInput): this;
}
