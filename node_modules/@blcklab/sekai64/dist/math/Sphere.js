import { Vector3 } from './Vector3.js';
export class Sphere {
    radius;
    center = new Vector3();
    constructor(center, radius = -1) {
        this.radius = radius;
        if (center)
            this.center.copy(center);
    }
}