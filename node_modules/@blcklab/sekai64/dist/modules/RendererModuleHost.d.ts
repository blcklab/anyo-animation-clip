import type { Camera } from '../cameras/index.js';
import type { Renderer, RendererDiagnostic } from '../renderer/index.js';
import type { Scene } from '../scene/index.js';
import type { InstalledRendererModule, RendererModule, RendererModuleCapabilities, RendererModuleFrame, RendererRecoveryContext } from './RendererModule.js';
export declare class RendererModuleHost {
    readonly renderer: Renderer;
    private readonly reportDiagnostic;
    private readonly states;
    private disposed;
    constructor(renderer: Renderer, reportDiagnostic?: (diagnostic: RendererDiagnostic) => void);
    installAll(modules: readonly RendererModule[]): Promise<void>;
    install(module: RendererModule): Promise<() => Promise<void>>;
    update(frame: RendererModuleFrame): void;
    beforeRender(scene: Scene, camera: Camera): void;
    afterRender(scene: Scene, camera: Camera): void;
    recover(context: RendererRecoveryContext): Promise<void>;
    has(id: string): boolean;
    get capabilities(): Readonly<Record<string, RendererModuleCapabilities>>;
    list(): readonly InstalledRendererModule[];
    uninstall(id: string): Promise<void>;
    disposeAsync(): Promise<void>;
    private assertAlive;
}
