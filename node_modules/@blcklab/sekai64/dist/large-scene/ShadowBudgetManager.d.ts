import type { Light } from '../lighting/index.js';
import type { Vector3 } from '../math/index.js';
export interface ShadowCandidate {
    light: Light & {
        castShadow?: boolean;
    };
    priority?: number;
    resolution?: number;
    distance?: number;
}
export interface ShadowBudget {
    maxLights: number;
    maxPixels: number;
    defaultResolution?: number;
}
export interface ShadowAllocation {
    light: Light;
    resolution: number;
    priority: number;
}
export declare class ShadowBudgetManager {
    readonly budget: ShadowBudget;
    constructor(budget: ShadowBudget);
    allocate(candidates: readonly ShadowCandidate[]): readonly ShadowAllocation[];
    static distance(lightPosition: Vector3, reference: Vector3): number;
}
