import { type RendererColorGrading, type RendererImageQuality, type RendererPostProcessing } from '../renderer/index.js';
export declare class WebGPUPostProcessPipeline {
    private readonly device;
    private color?;
    private depth?;
    private colorView?;
    private depthView?;
    private sampler?;
    private bindGroupLayout?;
    private pipelineLayout?;
    private pipeline?;
    private aoA?;
    private aoB?;
    private bloom;
    private params;
    private lutTexture?;
    private lutView?;
    private lutSource?;
    private fallbackLut?;
    private fallbackLutView?;
    private width;
    private height;
    private format;
    lastPassCount: number;
    constructor(device: GPUDevice);
    get targetColorView(): GPUTextureView;
    get targetDepthView(): GPUTextureView;
    ensure(width: number, height: number, format: GPUTextureFormat): void;
    composite(encoder: GPUCommandEncoder, target: GPUTextureView, configuration: RendererPostProcessing, imageQuality: RendererImageQuality, colorGrading: RendererColorGrading): void;
    dispose(): void;
    private draw;
    private ensurePipeline;
    private ensureLutTexture;
    private ensureFallbackLut;
    private ensureTarget;
    private releaseTargets;
}
