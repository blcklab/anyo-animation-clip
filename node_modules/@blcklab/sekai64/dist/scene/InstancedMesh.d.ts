import type { Geometry } from '../geometry/index.js';
import { Box3, Matrix4 } from '../math/index.js';
import { Mesh, type MeshOptions } from './Mesh.js';
export interface InstancedMeshOptions extends Omit<MeshOptions, 'geometry'> {
    geometry: Geometry;
    count: number;
    matrices?: Float32Array;
}
export declare class InstancedMesh extends Mesh {
    readonly count: number;
    readonly instanceMatrices: Float32Array;
    instanceVersion: number;
    private cachedBoundsVersion;
    private readonly cachedBounds;
    constructor(options: InstancedMeshOptions);
    setMatrixAt(index: number, matrix: Matrix4 | ArrayLike<number>): this;
    getMatrixAt(index: number, target?: Matrix4): Matrix4;
    computeLocalBounds(): Box3;
    clone(): InstancedMesh;
    private assertIndex;
}
