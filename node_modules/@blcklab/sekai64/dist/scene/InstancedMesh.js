import { Box3, Matrix4 } from '../math/index.js';
import { Mesh } from './Mesh.js';
export class InstancedMesh extends Mesh {
    count;
    instanceMatrices;
    instanceVersion = 0;
    cachedBoundsVersion = -1;
    cachedBounds = new Box3();
    constructor(options) { super(options); if (!Number.isInteger(options.count) || options.count < 1)
        throw new Error('InstancedMesh count must be a positive integer.'); this.count = options.count; this.instanceMatrices = options.matrices ?? new Float32Array(options.count * 16); if (this.instanceMatrices.length !== options.count * 16)
        throw new Error('InstancedMesh matrices length must equal count * 16.'); if (!options.matrices) {
        const identity = new Matrix4().elements;
        for (let i = 0; i < options.count; i++)
            this.instanceMatrices.set(identity, i * 16);
    } }
    setMatrixAt(index, matrix) { this.assertIndex(index); const source = matrix instanceof Matrix4 ? matrix.elements : matrix; if (source.length < 16)
        throw new Error('Instance matrix requires 16 values.'); for (let i = 0; i < 16; i++)
        this.instanceMatrices[index * 16 + i] = source[i] ?? 0; this.instanceVersion++; return this; }
    getMatrixAt(index, target = new Matrix4()) { this.assertIndex(index); target.elements.set(this.instanceMatrices.subarray(index * 16, index * 16 + 16)); return target; }
    computeLocalBounds() { if (this.cachedBoundsVersion === this.instanceVersion)
        return this.cachedBounds; this.cachedBounds.makeEmpty(); const matrix = new Matrix4(); for (let i = 0; i < this.count; i++) {
        this.getMatrixAt(i, matrix);
        this.cachedBounds.expandByBox(this.geometry.bounds.clone().applyMatrix4(matrix));
    } this.cachedBoundsVersion = this.instanceVersion; return this.cachedBounds; }
    clone() { const copy = new InstancedMesh({ id: this.id, name: this.name, tags: [...this.tags], visible: this.visible, layerMask: this.layerMask, geometry: this.geometry, materials: this.materials, materialGroupSlots: this.materialGroupSlots, ownsResources: false, castShadow: this.castShadow, receiveShadow: this.receiveShadow, count: this.count, matrices: new Float32Array(this.instanceMatrices) }); copy.position.copy(this.position); copy.rotation.copy(this.rotation); copy.scale.copy(this.scale); return copy; }
    assertIndex(index) { if (!Number.isInteger(index) || index < 0 || index >= this.count)
        throw new RangeError(`Instance index ${index} is outside 0..${this.count - 1}.`); }
}