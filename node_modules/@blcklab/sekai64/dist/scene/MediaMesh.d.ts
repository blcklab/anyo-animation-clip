import { Texture, type TextTextureOptions, type TextureLoadOptions, type TextureSource } from '../materials/index.js';
import type { ColorInput } from '../math/index.js';
import { Mesh } from './Mesh.js';
import type { NodeOptions } from './Node.js';
export interface TextMeshOptions extends NodeOptions, TextTextureOptions {
    worldWidth?: number;
    worldHeight?: number;
    tint?: ColorInput;
}
/** A four-vertex, unlit text surface backed by a generated canvas texture. */
export declare class TextMesh extends Mesh {
    text: string;
    readonly textOptions: TextMeshOptions;
    constructor(text: string, options?: TextMeshOptions);
    get texture(): Texture;
    setText(text: string, options?: Partial<TextMeshOptions>): this;
}
export interface ImageMeshOptions extends NodeOptions {
    autoload?: boolean;
    loadOptions?: TextureLoadOptions;
    worldWidth?: number;
    worldHeight?: number;
    tint?: ColorInput;
    transparent?: boolean;
    alphaCutoff?: number;
    flipY?: boolean;
    label?: string;
}
/** Lightweight image surface with dependency-free asynchronous decoding. */
export declare class ImageMesh extends Mesh {
    readonly texture: Texture;
    readonly imageOptions: ImageMeshOptions;
    readonly ready: Promise<this>;
    constructor(source: Texture | TextureSource, options?: ImageMeshOptions);
    static create(source: Texture | TextureSource, options?: ImageMeshOptions & TextureLoadOptions): Promise<ImageMesh>;
    setSource(source: TextureSource, options?: TextureLoadOptions): Promise<this>;
    load(options?: TextureLoadOptions): Promise<this>;
}
