import { ManagedResource } from '../core/index.js';
import { Color } from '../math/index.js';
export type ToneMapping = 'none' | 'reinhard' | 'aces';
export interface EnvironmentResourceOptions {
    id: string;
    width: number;
    height: number;
    pixels: Float32Array;
    intensity?: number;
    exposure?: number;
    toneMapping?: ToneMapping;
    label?: string;
}
export declare class EnvironmentResource extends ManagedResource {
    readonly id: string;
    readonly width: number;
    readonly height: number;
    readonly pixels: Float32Array;
    intensity: number;
    exposure: number;
    toneMapping: ToneMapping;
    readonly average: Color;
    readonly skyAverage: Color;
    readonly groundAverage: Color;
    constructor(options: EnvironmentResourceOptions);
    recalculateAverage(): this;
    toLdr(): Uint8ClampedArray;
    protected release(): void;
}
export declare function toneMap(value: number, mode: ToneMapping): number;
