import type { RendererModule, RendererModuleInstance } from '../modules/index.js';
import type { EnvironmentResource } from './EnvironmentResource.js';
export interface EnvironmentPresentation {
    background?: boolean;
    intensity?: number;
}
export declare class EnvironmentRendererModule implements RendererModule {
    readonly id = "sekai64.environment";
    readonly version: "0.8.0-rc.34-dev.2";
    private environment?;
    private presentation;
    private readonly light;
    private renderer?;
    setup(context: Parameters<RendererModule['setup']>[0]): RendererModuleInstance;
    setEnvironment(environment: EnvironmentResource | undefined, presentation?: EnvironmentPresentation): this;
    private apply;
    dispose(): void;
}
export declare function createEnvironmentRendererModule(): EnvironmentRendererModule;
