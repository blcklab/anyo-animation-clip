import type { Camera } from '../cameras/index.js';
import { Box3, Matrix4, Vector3 } from '../math/index.js';
import { type RendererShadowOptions } from './VisualPipeline.js';
export interface DirectionalShadowFrame {
    matrix: Matrix4;
    center: Vector3;
    radius: number;
}
export interface DirectionalShadowCascadeFrame extends DirectionalShadowFrame {
    index: number;
    splitNear: number;
    splitFar: number;
}
/** Builds a stable single-cascade directional-light matrix around visible scene bounds. */
export declare function createDirectionalShadowFrame(bounds: Box3, directionInput: readonly [number, number, number] | Vector3, padding?: number): DirectionalShadowFrame | undefined;
/**
 * Creates camera-frustum cascades for directional outdoor shadows.
 * The split scheme is renderer-neutral and the optional texel snapping keeps
 * the orthographic projection stable while the camera moves slowly.
 */
export declare function createDirectionalShadowCascades(camera: Camera, directionInput: readonly [number, number, number] | Vector3, options: Pick<RendererShadowOptions, 'cascades' | 'maxDistance' | 'splitLambda' | 'cameraPadding' | 'stabilize' | 'mapSize'>): readonly DirectionalShadowCascadeFrame[];
