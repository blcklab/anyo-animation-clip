export interface RendererColorLut {
    /** Edge length of the cubic LUT. Production LUTs commonly use 16, 32, or 64. */
    size: number;
    /** RGB triplets ordered with red changing fastest, then green, then blue. */
    data: Float32Array;
    label?: string;
}
export interface ParsedCubeLut {
    lut: RendererColorLut;
    domainMin: readonly [number, number, number];
    domainMax: readonly [number, number, number];
}
export declare function createIdentityColorLut(size?: number, label?: string): RendererColorLut;
export declare function parseCubeLut(source: string, label?: string): ParsedCubeLut;
export declare function sampleColorLut(lut: RendererColorLut, color: readonly [number, number, number], intensity?: number): [number, number, number];
/** Packs a cubic LUT into a portable 2D strip: width=size*size, height=size. */
export declare function packColorLutStrip(lut: RendererColorLut): {
    width: number;
    height: number;
    pixels: Uint8Array;
};
export declare function validateColorLut(lut: RendererColorLut): void;
