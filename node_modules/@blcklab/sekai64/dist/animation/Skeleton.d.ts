import { ManagedResource } from '../core/index.js';
import type { Node } from '../scene/index.js';
export interface SkeletonResourceOptions {
    id: string;
    joints: readonly Node[];
    inverseBindMatrices?: Float32Array;
    label?: string;
}
export declare class SkeletonResource extends ManagedResource {
    readonly id: string;
    readonly joints: readonly Node[];
    readonly inverseBindMatrices: Float32Array;
    readonly palette: Float32Array;
    version: number;
    private readonly inverse;
    private readonly combined;
    constructor(options: SkeletonResourceOptions);
    update(): this;
    protected release(): void;
}
