import { AnimationClip } from './AnimationClip.js';
import { AnimationMixer } from './AnimationMixer.js';
import { SkeletonResource } from './Skeleton.js';
import { SkinnedGeometry } from './SkinnedGeometry.js';
import { SEKAI64_MODULE_VERSION } from '../modules/index.js';
export class AnimationRendererModule {
    options;
    id = 'sekai64.animation';
    version = SEKAI64_MODULE_VERSION;
    mixers = new Set();
    bindings = new Set();
    installed = false;
    disposed = false;
    context;
    constructor(options = {}) {
        this.options = options;
    }
    setup(context) {
        if (this.installed)
            throw new Error('AnimationRendererModule is already installed.');
        if (this.disposed)
            throw new Error('AnimationRendererModule is disposed.');
        this.installed = true;
        this.context = context;
        const maxJoints = Math.max(1, Math.floor(this.options.maxJoints ?? (context.renderer.backend === 'webgpu' ? 256 : 128)));
        const maxMorphTargets = Math.max(1, Math.floor(this.options.maxMorphTargets ?? 16));
        return {
            capabilities: {
                skinning: true,
                morphTargets: true,
                clipSampling: true,
                mixers: true,
                crossFades: true,
                maxJoints,
                maxMorphTargets,
                deformation: 'cpu-dynamic-geometry',
                shaderVariants: Object.freeze(['static', 'skinned', 'morph', 'skinned-morph']),
            },
            update: frame => this.update(frame.deltaTime),
            recover: () => { for (const binding of this.bindings)
                binding.mesh.geometry.markUpdated(false); },
            dispose: () => this.dispose(),
        };
    }
    createMixer(root, clips = []) {
        this.assertAlive();
        const mixer = new AnimationMixer(root, clips);
        this.mixers.add(mixer);
        return mixer;
    }
    removeMixer(mixer) { this.mixers.delete(mixer); }
    bind(mesh, skeleton) {
        this.assertAlive();
        if (!(mesh.geometry instanceof SkinnedGeometry))
            throw new Error('Animation module bindings require SkinnedGeometry.');
        if (skeleton && skeleton.joints.length > (this.options.maxJoints ?? (this.context?.renderer.backend === 'webgpu' ? 256 : 128))) {
            throw new Error(`Skeleton ${skeleton.id} exceeds the configured joint limit.`);
        }
        const binding = { mesh, ...(skeleton ? { skeleton } : {}) };
        this.bindings.add(binding);
        return () => this.bindings.delete(binding);
    }
    update(deltaTime) {
        this.assertAlive();
        for (const mixer of this.mixers)
            mixer.update(deltaTime);
        for (const binding of this.bindings) {
            if (binding.mesh.disposed || binding.mesh.geometry.disposed || binding.skeleton?.disposed) {
                this.bindings.delete(binding);
                continue;
            }
            binding.mesh.updateWorldFromRoot();
            binding.mesh.geometry.deform(binding.skeleton, binding.mesh.worldMatrix);
        }
    }
    dispose() {
        if (this.disposed)
            return;
        this.disposed = true;
        for (const mixer of this.mixers) {
            mixer.stopAll();
            mixer.clearListeners();
        }
        this.mixers.clear();
        this.bindings.clear();
        this.context = undefined;
    }
    assertAlive() { if (this.disposed)
        throw new Error('AnimationRendererModule is disposed.'); }
}
export function createAnimationRendererModule(options = {}) {
    return new AnimationRendererModule(options);
}