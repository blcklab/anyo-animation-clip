import { EventDispatcher } from '../core/index.js';
export class AssetLease {
    key;
    value;
    releaseValue;
    disposed = false;
    constructor(key, value, releaseValue) {
        this.key = key;
        this.value = value;
        this.releaseValue = releaseValue;
    }
    dispose() { if (this.disposed)
        return; this.disposed = true; this.releaseValue(); }
}
export class AssetManager extends EventDispatcher {
    disposed = false;
    baseUrl;
    allowedProtocols;
    cache = new Map();
    resolvers = [];
    queue = [];
    active = 0;
    concurrency;
    constructor(options = {}) {
        super();
        this.baseUrl = options.baseUrl ? new URL(options.baseUrl, defaultBaseUrl()) : undefined;
        this.concurrency = Math.max(1, Math.floor(options.concurrency ?? 6));
        this.allowedProtocols = new Set(options.allowedProtocols ?? ['http:', 'https:', 'data:', 'blob:', 'file:']);
    }
    addResolver(resolver) { this.resolvers.unshift(resolver); return () => { const index = this.resolvers.indexOf(resolver); if (index >= 0)
        this.resolvers.splice(index, 1); }; }
    async loadArrayBuffer(source, options = {}) {
        return this.load(source, 'arrayBuffer', options, async (response) => readResponse(response, options), value => value.byteLength);
    }
    async loadText(source, options = {}) {
        return this.load(source, 'text', options, async (response) => new TextDecoder().decode(await readResponse(response, options)), value => new TextEncoder().encode(value).byteLength);
    }
    async loadJson(source, options = {}) {
        const lease = await this.loadText(source, options);
        try {
            const parsed = JSON.parse(lease.value);
            return new AssetLease(lease.key, parsed, () => lease.dispose());
        }
        catch (error) {
            lease.dispose();
            throw new Error(`Sekai64 failed to parse JSON asset ${lease.key}: ${error instanceof Error ? error.message : String(error)}`);
        }
    }
    async loadBlob(source, options = {}) {
        return this.load(source, 'blob', options, async (response) => new Blob([await readResponse(response, options)], { type: response.headers.get('content-type') ?? 'application/octet-stream' }), value => value.size);
    }
    async loadImageBitmap(source, options = {}) {
        if (typeof createImageBitmap !== 'function')
            throw new Error('ImageBitmap is unavailable in this environment.');
        return this.load(source, 'imageBitmap', options, async (response) => createImageBitmap(new Blob([await readResponse(response, options)], { type: response.headers.get('content-type') ?? 'application/octet-stream' })), value => value.width * value.height * 4, value => value.close());
    }
    clearUnused() {
        for (const [key, entry] of this.cache)
            if (entry.references <= 0 && entry.value !== undefined) {
                entry.disposer?.(entry.value);
                this.cache.delete(key);
                this.emit('released', { key });
            }
    }
    clear() { for (const entry of this.cache.values())
        if (entry.value !== undefined)
            entry.disposer?.(entry.value); this.cache.clear(); }
    get stats() {
        let references = 0, bytes = 0;
        for (const entry of this.cache.values()) {
            references += entry.references;
            bytes += entry.bytes;
        }
        return { entries: this.cache.size, references, bytes, active: this.active, queued: this.queue.length };
    }
    dispose() {
        if (this.disposed)
            return;
        this.disposed = true;
        this.clear();
        const error = new Error('AssetManager was disposed before a queued asset load could start.');
        for (const item of this.queue.splice(0))
            item.reject(error);
        this.resolvers.length = 0;
        this.clearListeners();
    }
    async load(source, kind, options, decode, sizeOf, disposer) {
        if (this.disposed)
            throw new Error('AssetManager is disposed.');
        throwIfAborted(options.signal);
        const url = this.resolve(source);
        const key = `${kind}:${url.href}`;
        const useCache = options.cache !== false;
        let entry = (useCache ? this.cache.get(key) : undefined);
        if (!entry) {
            entry = { references: 0, bytes: 0, disposer, promise: this.schedule(async () => {
                    const response = await this.fetchWithRetry(url, options);
                    const value = await decode(response);
                    entry.value = value;
                    entry.bytes = sizeOf(value);
                    this.emit('loaded', { key, bytes: entry.bytes });
                    return value;
                }) };
            entry.promise.catch(error => { if (useCache)
                this.cache.delete(key); this.emit('error', { key, error }); });
            if (useCache)
                this.cache.set(key, entry);
        }
        const value = await withAbort(entry.promise, options.signal);
        entry.references += 1;
        let released = false;
        return new AssetLease(key, value, () => {
            if (released)
                return;
            released = true;
            entry.references = Math.max(0, entry.references - 1);
            if (!useCache && entry.references === 0)
                entry.disposer?.(value);
        });
    }
    resolve(source) {
        const url = source instanceof URL ? new URL(source.href) : new URL(source, this.baseUrl ?? defaultBaseUrl());
        if (!this.allowedProtocols.has(url.protocol))
            throw new Error(`Asset protocol is not allowed: ${url.protocol}`);
        return url;
    }
    async fetchWithRetry(url, options) {
        const retries = Math.max(0, Math.floor(options.retries ?? 1));
        let lastError;
        for (let attempt = 0; attempt <= retries; attempt += 1) {
            throwIfAborted(options.signal);
            try {
                const resolver = this.resolvers.find(candidate => candidate.canResolve(url));
                const response = resolver ? await resolver.fetch(url, options) : await fetch(url, { signal: options.signal, headers: options.headers });
                if (!response.ok)
                    throw new Error(`HTTP ${response.status} ${response.statusText}`);
                return response;
            }
            catch (error) {
                lastError = error;
                if (attempt >= retries || options.signal?.aborted)
                    break;
                await delay(Math.min(1000, 100 * 2 ** attempt), options.signal);
            }
        }
        throw new Error(`Sekai64 asset load failed for ${url.href}: ${lastError instanceof Error ? lastError.message : String(lastError)}`);
    }
    schedule(task) {
        return new Promise((resolve, reject) => {
            const run = () => {
                if (this.disposed) {
                    reject(new Error('AssetManager is disposed.'));
                    return;
                }
                this.active += 1;
                void task().then(resolve, reject).finally(() => {
                    this.active -= 1;
                    this.queue.shift()?.run();
                });
            };
            if (this.active < this.concurrency)
                run();
            else
                this.queue.push({ run, reject });
        });
    }
}
async function readResponse(response, options) {
    const totalHeader = response.headers.get('content-length');
    const total = totalHeader ? Number(totalHeader) : undefined;
    if (!response.body || !options.onProgress)
        return response.arrayBuffer();
    const reader = response.body.getReader();
    const chunks = [];
    let loaded = 0;
    while (true) {
        throwIfAborted(options.signal);
        const { done, value } = await reader.read();
        if (done)
            break;
        chunks.push(value);
        loaded += value.byteLength;
        options.onProgress({ loaded, total, ratio: total && total > 0 ? loaded / total : undefined });
    }
    const output = new Uint8Array(loaded);
    let offset = 0;
    for (const chunk of chunks) {
        output.set(chunk, offset);
        offset += chunk.byteLength;
    }
    return output.buffer;
}
function defaultBaseUrl() { return typeof location !== 'undefined' ? location.href : 'file:///'; }
function throwIfAborted(signal) { if (signal?.aborted)
    throw signal.reason ?? new DOMException('The operation was aborted.', 'AbortError'); }
function withAbort(promise, signal) {
    if (!signal)
        return promise;
    throwIfAborted(signal);
    return new Promise((resolve, reject) => {
        const abort = () => { cleanup(); reject(signal.reason ?? new DOMException('The operation was aborted.', 'AbortError')); };
        const cleanup = () => signal.removeEventListener('abort', abort);
        signal.addEventListener('abort', abort, { once: true });
        void promise.then(value => { cleanup(); resolve(value); }, error => { cleanup(); reject(error); });
    });
}
function delay(milliseconds, signal) { return new Promise((resolve, reject) => { const handle = setTimeout(resolve, milliseconds); signal?.addEventListener('abort', () => { clearTimeout(handle); reject(signal.reason); }, { once: true }); }); }