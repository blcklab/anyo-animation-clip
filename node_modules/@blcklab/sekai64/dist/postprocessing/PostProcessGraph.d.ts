import type { Disposable } from '../core/index.js';
export interface PostProcessContext<TFrame> {
    frame: TFrame;
    width: number;
    height: number;
    deltaTime: number;
}
export interface PostProcessPass<TFrame> extends Partial<Disposable> {
    readonly name: string;
    enabled: boolean;
    process(context: PostProcessContext<TFrame>): TFrame | Promise<TFrame>;
}
export declare class PostProcessGraph<TFrame> implements Disposable {
    disposed: boolean;
    readonly passes: PostProcessPass<TFrame>[];
    add(...passes: readonly PostProcessPass<TFrame>[]): this;
    remove(pass: PostProcessPass<TFrame>): boolean;
    process(frame: TFrame, width: number, height: number, deltaTime?: number): Promise<TFrame>;
    dispose(): void;
    private assertAlive;
}
