import type { PostProcessContext, PostProcessPass } from './PostProcessGraph.js';
export type ToneMappingMode = 'linear' | 'reinhard' | 'aces' | 'neutral';
export interface ToneMappingOptions {
    mode?: ToneMappingMode;
    exposure?: number;
    gamma?: number;
}
export declare class ToneMappingPass implements PostProcessPass<ImageData> {
    readonly name = "tone-mapping";
    enabled: boolean;
    mode: ToneMappingMode;
    exposure: number;
    gamma: number;
    constructor(options?: ToneMappingOptions);
    process({ frame }: PostProcessContext<ImageData>): ImageData;
}
export interface BloomOptions {
    threshold?: number;
    intensity?: number;
    radius?: number;
}
export declare class BloomPass implements PostProcessPass<ImageData> {
    readonly name = "bloom";
    enabled: boolean;
    threshold: number;
    intensity: number;
    radius: number;
    constructor(options?: BloomOptions);
    process({ frame, width, height }: PostProcessContext<ImageData>): ImageData;
}
export declare function toneMapColor(color: readonly [number, number, number], mode?: ToneMappingMode, exposure?: number): [number, number, number];
