export interface WorkerTaskExecutor<Input, Output> {
    (input: Input, context: {
        signal: AbortSignal;
    }): Promise<Output>;
}
export declare class WorkerTaskPool<Input, Output> {
    readonly executeTask: WorkerTaskExecutor<Input, Output>;
    private readonly controllers;
    private disposed;
    constructor(executeTask: WorkerTaskExecutor<Input, Output>);
    run(input: Input, signal?: AbortSignal): Promise<Output>;
    dispose(): void;
}
