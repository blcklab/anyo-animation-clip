import { AssetTaskScheduler } from './AssetTaskScheduler.js';
import { SEKAI64_MODULE_VERSION } from '../modules/index.js';
export class StreamingRendererModule {
    id = 'sekai64.streaming';
    version = SEKAI64_MODULE_VERSION;
    scheduler;
    constructor(options = {}) { this.scheduler = new AssetTaskScheduler(options.concurrency ?? 4); }
    setup() { return { capabilities: { backgroundLoading: true, cancellation: true, requestDeduplication: true, referenceCounting: true, workerCompatible: true }, dispose: () => this.scheduler.dispose() }; }
}
export function createStreamingRendererModule(options = {}) { return new StreamingRendererModule(options); }