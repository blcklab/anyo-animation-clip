import { Box3, Vector3 } from '../math/index.js';
import { Mesh, type Node, type Scene } from '../scene/index.js';
export interface StaticCollider {
    readonly id: string;
    readonly bounds: Box3;
    readonly node?: Node;
    readonly enabled: boolean;
}
export interface AddBoxColliderOptions {
    id: string;
    min: Vector3 | readonly [number, number, number];
    max: Vector3 | readonly [number, number, number];
    node?: Node;
    enabled?: boolean;
}
export declare class CollisionWorld {
    readonly cellSize: number;
    private readonly colliders;
    private readonly cells;
    constructor(cellSize?: number);
    addBox(options: AddBoxColliderOptions): StaticCollider;
    addMesh(mesh: Mesh, id?: string): StaticCollider;
    addScene(scene: Scene, predicate?: (mesh: Mesh) => boolean): number;
    remove(id: string): boolean;
    clear(): void;
    get(id: string): StaticCollider | undefined;
    get size(): number;
    query(bounds: Box3): StaticCollider[];
    private index;
    private cell;
}
