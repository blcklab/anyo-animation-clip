"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.AnyoAnimationPlugin = void 0;
exports.createAnimationPlugin = createAnimationPlugin;
const AnimationController_js_1 = require("./AnimationController.js");
class AnyoAnimationPlugin {
    options;
    name = 'anyo:animation';
    controller;
    availabilityCleanup = null;
    activeContext = null;
    cleanups = [];
    pendingDispose = null;
    constructor(options) {
        this.options = options;
        this.controller = new AnimationController_js_1.AnimationController(options.adapter, options);
    }
    async setup(context) {
        this.activeContext = context;
        this.pendingDispose = null;
        await this.options.adapter.setup?.(context);
        this.availabilityCleanup?.();
        this.availabilityCleanup = this.options.adapter.onAvailabilityChange?.(() => {
            if (this.activeContext)
                this.controller.reconcile(this.activeContext);
        }) ?? null;
        this.bindHostLifecycle(context);
        this.controller.bind(context);
    }
    update(deltaSeconds) { this.controller.update(deltaSeconds); }
    applyChanges(_changes, context) {
        this.activeContext = context;
        this.controller.reconcile(context);
    }
    /** Releases bindings/listeners for the current world document while keeping the adapter reusable. */
    teardown() { this.teardownRuntime(); }
    /** Permanently releases the plugin and its renderer adapter. */
    dispose() { void this.disposeAsync(); }
    async disposeAsync() {
        if (this.pendingDispose)
            return this.pendingDispose;
        this.pendingDispose = (async () => {
            this.teardownRuntime();
            await this.options.adapter.dispose?.();
        })();
        return this.pendingDispose;
    }
    play(...args) { return this.controller.play(...args); }
    crossFade(...args) { return this.controller.crossFade(...args); }
    transitionTo(...args) { return this.controller.transitionTo(...args); }
    pause(...args) { return this.controller.pause(...args); }
    resume(...args) { return this.controller.resume(...args); }
    stop(...args) { return this.controller.stop(...args); }
    seek(...args) { return this.controller.seek(...args); }
    setSpeed(...args) { return this.controller.setSpeed(...args); }
    setBoolean(...args) { return this.controller.setBoolean(...args); }
    setNumber(...args) { return this.controller.setNumber(...args); }
    setTrigger(...args) { return this.controller.setTrigger(...args); }
    resetTrigger(...args) { return this.controller.resetTrigger(...args); }
    setParameter(...args) { return this.controller.setParameter(...args); }
    getParameter(...args) { return this.controller.getParameter(...args); }
    subscribeParameter(...args) { return this.controller.subscribeParameter(...args); }
    bindParameter(...args) { return this.controller.bindParameter(...args); }
    createParameterSnapshot(...args) { return this.controller.createParameterSnapshot(...args); }
    restoreParameterSnapshot(...args) { return this.controller.restoreParameterSnapshot(...args); }
    createSnapshot() { return this.controller.createSnapshot(); }
    restoreSnapshot(...args) { return this.controller.restoreSnapshot(...args); }
    pauseAll() { this.controller.pauseAll(); }
    resumeAll() { this.controller.resumeAll(); }
    stopAll() { this.controller.stopAll(); }
    get(...args) { return this.controller.get(...args); }
    list() { return this.controller.list(); }
    teardownRuntime() {
        this.activeContext = null;
        this.availabilityCleanup?.();
        this.availabilityCleanup = null;
        for (const cleanup of this.cleanups.splice(0))
            cleanup();
        this.controller.dispose();
    }
    bindHostLifecycle(context) {
        for (const cleanup of this.cleanups.splice(0))
            cleanup();
        const world = context.world;
        if (typeof world.on === 'function') {
            this.cleanups.push(world.on('player:pause', () => this.controller.pauseAll()), world.on('player:resume', () => this.controller.resumeAll()), world.on('world:pause', () => this.controller.pauseAll()), world.on('world:resume', () => this.controller.resumeAll()), world.on('renderer:recovered', () => this.activeContext && this.controller.reconcile(this.activeContext)), world.on('renderer:recovery-complete', () => this.activeContext && this.controller.reconcile(this.activeContext)), world.on('player:world-replaced', () => this.activeContext && this.controller.reconcile(this.activeContext)));
        }
        if (typeof world.registerAction === 'function') {
            this.cleanups.push(world.registerAction('animation.play', (params) => {
                this.controller.play(requiredEntity(params), requiredString(params.clip, 'clip'), {
                    ...(typeof params.loop === 'string' ? { loop: params.loop } : {}),
                    ...(typeof params.speed === 'number' ? { speed: params.speed } : {}),
                    ...(typeof params.startTime === 'number' ? { startTime: params.startTime } : {}),
                });
            }), world.registerAction('animation.crossfade', (params) => {
                this.controller.crossFade(requiredEntity(params), requiredString(params.clip, 'clip'), {
                    ...(typeof params.duration === 'number' ? { duration: params.duration } : {}),
                    ...(typeof params.loop === 'string' ? { loop: params.loop } : {}),
                    ...(typeof params.speed === 'number' ? { speed: params.speed } : {}),
                    ...(typeof params.startTime === 'number' ? { startTime: params.startTime } : {}),
                });
            }), world.registerAction('animation.state', (params) => {
                this.controller.transitionTo(requiredEntity(params), requiredString(params.state, 'state'), typeof params.duration === 'number' ? params.duration : 0.2);
            }), world.registerAction('animation.parameter', (params) => {
                const value = params.value;
                if (typeof value !== 'boolean' && typeof value !== 'number')
                    throw new Error('value must be boolean or number.');
                this.controller.setParameter(requiredEntity(params), requiredString(params.parameter, 'parameter'), value);
            }), world.registerAction('animation.trigger', (params) => {
                this.controller.setTrigger(requiredEntity(params), requiredString(params.parameter, 'parameter'));
            }));
        }
    }
}
exports.AnyoAnimationPlugin = AnyoAnimationPlugin;
function createAnimationPlugin(options) {
    return new AnyoAnimationPlugin(options);
}
function requiredEntity(params) { return requiredString(params.entityId, 'entityId'); }
function requiredString(value, field) {
    if (typeof value !== 'string' || !value.trim())
        throw new Error(`${field} must be a non-empty string.`);
    return value;
}
//# sourceMappingURL=plugin.js.map