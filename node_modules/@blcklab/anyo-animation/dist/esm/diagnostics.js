export function createAnimationDiagnostic(severity, code, message, entityId, details) {
    return Object.freeze({
        severity,
        code,
        message,
        ...(entityId ? { entityId } : {}),
        ...(details ? { details: Object.freeze({ ...details }) } : {}),
    });
}
//# sourceMappingURL=diagnostics.js.map