import { InteractionManager } from './InteractionManager.js';
export function interactionsPlugin(options = {}) {
    let manager = null;
    return {
        name: 'anyo:interactions',
        setup(context) {
            manager = new InteractionManager(context, options);
        },
        dispose() {
            manager?.dispose();
            manager = null;
        },
    };
}
//# sourceMappingURL=plugin.js.map