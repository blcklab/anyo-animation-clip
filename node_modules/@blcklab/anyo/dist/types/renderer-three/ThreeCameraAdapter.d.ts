import type { CameraAdapter, CanvasProjection, Vec3 } from '../core/types.js';
export declare class ThreeCameraAdapter implements CameraAdapter {
    private readonly camera;
    private readonly canvas?;
    constructor(camera: any, canvas?: HTMLCanvasElement | undefined);
    getPosition(): Vec3;
    setPosition(position: Vec3): void;
    getRotation(): readonly [number, number];
    setRotation(yaw: number, pitch: number): void;
    getForward(): Vec3;
    getRight(): Vec3;
    projectWorldPoint(point: Vec3): CanvasProjection;
}
//# sourceMappingURL=ThreeCameraAdapter.d.ts.map