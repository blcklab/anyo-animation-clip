import type { CompiledPrimitive, CompiledWorld, NormalizedWorldDocument, RendererAdapter, RuntimeTransformUpdate } from '../core/types.js';
import { ThreeCameraAdapter } from './ThreeCameraAdapter.js';
export interface ThreeRendererOptions {
    canvas: HTMLCanvasElement;
    antialias?: boolean;
    alpha?: boolean;
    shadows?: boolean;
    fieldOfView?: number;
    near?: number;
    far?: number;
    toneMappingExposure?: number;
}
export declare class ThreeRenderer implements RendererAdapter {
    private readonly options;
    readonly canvas: HTMLCanvasElement;
    readonly camera: ThreeCameraAdapter;
    readonly info: {
        readonly name: "three";
        readonly capabilities: {
            readonly models: true;
            readonly text: true;
            readonly images: true;
            readonly lights: true;
            readonly ambientLights: true;
            readonly directionalLights: true;
            readonly pointLights: true;
            readonly picking: true;
            readonly trianglePicking: true;
            readonly instancedPicking: false;
            readonly roomVisibility: true;
            readonly incrementalUpdates: true;
            readonly runtimeTransforms: true;
            readonly instancing: false;
            readonly shadows: true;
            readonly xr: false;
            readonly backend: "webgl";
            readonly materialFeatures: readonly ["normalScale", "occlusionStrength", "emissiveIntensity", "transmission", "ior", "thickness", "attenuation"];
            readonly colorManagement: true;
            readonly environmentLighting: true;
            readonly environmentMaps: true;
        };
    };
    private readonly webgl;
    private readonly scene;
    private readonly perspectiveCamera;
    private readonly root;
    private readonly raycaster;
    private readonly pointer;
    private readonly objects;
    private readonly roomGroups;
    private readonly materialCache;
    private document;
    private compiled;
    private disposed;
    constructor(options: ThreeRendererOptions);
    mount(compiled: CompiledWorld, document: NormalizedWorldDocument): Promise<void>;
    updatePrimitive(primitive: CompiledPrimitive): Promise<void>;
    applyChanges(changes: readonly import('../core/types.js').WorldChange[]): Promise<void>;
    applyRuntimeTransforms(updates: readonly RuntimeTransformUpdate[]): void;
    removePrimitive(primitiveId: string): Promise<void>;
    setPrimitiveVisibility(primitiveId: string, visible: boolean): void;
    setRoomVisibility(roomId: string, visible: boolean): void;
    render(): void;
    resize(width: number, height: number, pixelRatio?: number): void;
    pick(clientX: number, clientY: number): {
        entityId?: string;
        primitiveId: string;
    } | null;
    dispose(): void;
    private addPrimitive;
    private createObject;
    private loadModel;
    private createFallbackModel;
    private createLight;
    private getMaterial;
    private createMaterialCacheKey;
    private defaultMaterial;
    private createStandardMaterial;
    private installEnvironment;
    private clearMaterialCache;
    private clearWorld;
    private disposeObject;
    private materialCacheHas;
    private assertNotDisposed;
    private disposeMaterial;
}
//# sourceMappingURL=ThreeRenderer.d.ts.map