import type { Engine, Scene } from '@blcklab/sekai64';
import { type XRFrameState } from '@blcklab/sekai64/xr';
import type { RendererXRBridge, RendererXRCapabilities, RendererXREnterOptions, RendererXREventMap, XRInputSnapshot, XRPlayerRigTransform, XRSessionMode, XRSessionState, XRViewerPoseSnapshot } from '../core/types.js';
import { Sekai64FrameDriver } from './Sekai64FrameDriver.js';
interface Sekai64XRBridgeOptions {
    ensureEngine(): Promise<Engine>;
    getScene(): Scene | null;
    setFrameState(state: XRFrameState | null): void;
    frameDriver: Sekai64FrameDriver;
}
export declare class Sekai64XRBridge implements RendererXRBridge {
    private readonly options;
    private manager;
    private layerBridge;
    private readonly listeners;
    private readonly support;
    private readonly referenceSpaces;
    private cleanups;
    private disposed;
    private backend;
    constructor(options: Sekai64XRBridgeOptions);
    get state(): XRSessionState;
    get capabilities(): RendererXRCapabilities;
    isSessionSupported(mode: XRSessionMode): Promise<boolean>;
    enter(options: RendererXREnterOptions): Promise<void>;
    exit(): Promise<void>;
    getViewerPose(): XRViewerPoseSnapshot | null;
    getInputSources(): readonly XRInputSnapshot[];
    getPlayerRigTransform(): XRPlayerRigTransform;
    setPlayerRigTransform(transform: XRPlayerRigTransform): void;
    render(scene: Scene, state: XRFrameState): void;
    on<TKey extends keyof RendererXREventMap>(event: TKey, listener: (payload: RendererXREventMap[TKey]) => void): () => void;
    dispose(): void;
    disposeAsync(): Promise<void>;
    private ensureManager;
    private emit;
    private assertAlive;
}
export {};
//# sourceMappingURL=Sekai64XRBridge.d.ts.map