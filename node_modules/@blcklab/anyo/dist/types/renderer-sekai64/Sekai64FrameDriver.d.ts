import type { XRFrameState, XRSessionManager } from '@blcklab/sekai64/xr';
import type { RendererFrameDriver } from '../core/types.js';
export declare class Sekai64FrameDriver implements RendererFrameDriver {
    private callback;
    private running;
    private windowHandle;
    private manager;
    private frameSink;
    get mode(): 'window' | 'xr';
    start(callback: (time: number) => void): void;
    stop(): void;
    activateXR(manager: XRSessionManager, frameSink: (state: XRFrameState | null) => void): void;
    deactivateXR(): void;
    dispose(): void;
    private startXRLoop;
    private startWindowLoop;
    private stopWindowLoop;
}
//# sourceMappingURL=Sekai64FrameDriver.d.ts.map