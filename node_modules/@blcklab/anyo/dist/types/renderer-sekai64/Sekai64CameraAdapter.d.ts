import { type Camera } from '@blcklab/sekai64';
import type { CameraAdapter, CameraProjection, CanvasProjection, Vec3 } from '../core/types.js';
export declare class Sekai64CameraAdapter implements CameraAdapter {
    private readonly resolveNativeCamera;
    private readonly applyProjection;
    private readonly canvas?;
    constructor(resolveNativeCamera: () => Camera, applyProjection: (projection: CameraProjection) => void, canvas?: HTMLCanvasElement | undefined);
    get nativeCamera(): Camera;
    getPosition(): Vec3;
    setPosition(position: Vec3): void;
    getRotation(): readonly [number, number];
    setRotation(yaw: number, pitch: number): void;
    getForward(): Vec3;
    getRight(): Vec3;
    getProjection(): CameraProjection;
    setProjection(projection: CameraProjection): void;
    projectWorldPoint(point: Vec3): CanvasProjection;
}
//# sourceMappingURL=Sekai64CameraAdapter.d.ts.map