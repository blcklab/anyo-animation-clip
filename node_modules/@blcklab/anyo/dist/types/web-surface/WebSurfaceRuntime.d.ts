import type { CompiledPrimitive, CompiledWebSurface, PluginRuntimeContext } from '../core/types.js';
import type { WebSurfaceAppRegistry } from './registry.js';
export interface WebSurfaceDiagnostic {
    severity: 'info' | 'warning' | 'error';
    code: string;
    message: string;
    entityId?: string;
    primitiveId?: string;
    details?: Readonly<Record<string, unknown>>;
}
export interface WebSurfaceExternalUrlPolicy {
    allowedOrigins: readonly string[];
    sandbox?: readonly string[];
    allow?: string;
    referrerPolicy?: ReferrerPolicy;
}
export interface WebSurfaceRuntimeOptions {
    registry: WebSurfaceAppRegistry;
    root?: HTMLElement;
    zIndex?: number;
    externalUrls?: WebSurfaceExternalUrlPolicy;
    onDiagnostic?: (diagnostic: WebSurfaceDiagnostic) => void;
    /** Generic host-controlled presentation filter used by optional presentation packages. */
    shouldPresent?: (primitive: CompiledPrimitive & {
        webSurface: CompiledWebSurface;
    }, context: PluginRuntimeContext) => boolean;
    /** Hides the renderer snapshot/plane while a live DOM presentation owns the surface. Defaults to true. */
    hideFallbackWhileActive?: boolean;
}
export declare class WebSurfaceRuntime {
    private readonly options;
    readonly capabilities: {
        readonly registeredApps: true;
        readonly snapshots: true;
        readonly domOverlay: boolean;
        readonly externalUrls: boolean;
        readonly liveXRPresentation: false;
    };
    private readonly mounted;
    private readonly ownsRoot;
    private readonly root;
    private disposed;
    private reducedMotion;
    constructor(options: WebSurfaceRuntimeOptions);
    sync(context: PluginRuntimeContext): Promise<void>;
    update(context: PluginRuntimeContext): void;
    dispose(): void;
    private mount;
    private updateMounted;
    private remountContent;
    private mountContent;
    private mountExternalLink;
    private mountExternalUrl;
    private canProject;
    private applyLayout;
    private setActive;
    private setFallbackHidden;
    private applyInteractionState;
    private releaseFocus;
    private installAnimationEvents;
    private play;
    private unmount;
    private disposeApp;
    private invoke;
    private diagnoseTargetFallback;
    private diagnosticOnce;
    private diagnostic;
}
//# sourceMappingURL=WebSurfaceRuntime.d.ts.map