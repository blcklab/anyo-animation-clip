import type { CameraAdapter, CompiledWorld, NormalizedWorldDocument, PickResult, RendererAdapter, RendererInfo, Vec3, WorldChange, RuntimeTransformUpdate } from './types.js';
declare class HeadlessCamera implements CameraAdapter {
    private position;
    private rotation;
    getPosition(): Vec3;
    setPosition(position: Vec3): void;
    getRotation(): readonly [number, number];
    setRotation(yaw: number, pitch: number): void;
    getForward(): Vec3;
    getRight(): Vec3;
}
export declare class HeadlessRenderer implements RendererAdapter {
    readonly isHeadless = true;
    readonly canvas: HTMLCanvasElement;
    readonly camera: HeadlessCamera;
    readonly info: RendererInfo;
    mount(_compiled: CompiledWorld, _document: NormalizedWorldDocument): void;
    applyChanges(_changes: readonly WorldChange[], _compiled: CompiledWorld, _document: NormalizedWorldDocument): void;
    applyRuntimeTransforms(_updates: readonly RuntimeTransformUpdate[]): void;
    setRoomVisibility(_roomId: string, _visible: boolean): void;
    render(_deltaSeconds: number): void;
    resize(_width: number, _height: number, _pixelRatio?: number): void;
    pick(_clientX: number, _clientY: number): PickResult | null;
    dispose(): void;
}
export declare function isHeadlessRenderer(renderer: RendererAdapter): renderer is HeadlessRenderer;
export {};
//# sourceMappingURL=HeadlessRenderer.d.ts.map