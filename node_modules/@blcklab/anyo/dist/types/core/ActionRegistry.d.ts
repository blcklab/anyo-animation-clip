import type { ActionHandler, WorldLike } from './types.js';
export declare class ActionRegistry {
    private readonly world;
    private readonly handlers;
    constructor(world: WorldLike);
    register(name: string, handler: ActionHandler): () => void;
    has(name: string): boolean;
    run(name: string, params?: Record<string, unknown>, source?: string): Promise<void>;
    clear(): void;
}
//# sourceMappingURL=ActionRegistry.d.ts.map