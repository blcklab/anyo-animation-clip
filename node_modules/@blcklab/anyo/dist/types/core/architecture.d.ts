import type { CameraDefinition, CompiledCamera, CompiledChannelMap, CompiledWorldChannels, CompilerDependencyGraph, ExtensionManifest, ExtensionRequirementDefinition, ExtensionResolutionDiagnostic, ExtensionValidationDiagnostic, NormalizedWorldDocument, RenderingHostPolicy, RenderingIntentDefinition, ResolvedRenderingConfiguration, RuntimeRenderingCapabilities, WorldChannelsDefinition, WorldDocument } from './types.js';
export declare function compileWorldChannels(definition?: WorldChannelsDefinition): CompiledWorldChannels;
export declare function resolveChannelMask(names: readonly string[] | undefined, map: CompiledChannelMap, fallback?: number): number;
export declare function compileCameras(cameras?: Readonly<Record<string, CameraDefinition>>, activeCamera?: string, channels?: CompiledWorldChannels): {
    cameras: CompiledCamera[];
    activeCameraId?: string;
};
export declare function resolveRenderingConfiguration(intent?: RenderingIntentDefinition, host?: RenderingHostPolicy, capabilities?: RuntimeRenderingCapabilities): ResolvedRenderingConfiguration;
export declare class ExtensionRegistry {
    private readonly manifests;
    private readonly ownership;
    register(manifest: ExtensionManifest): () => void;
    has(id: string): boolean;
    get(id: string): ExtensionManifest | undefined;
    list(): readonly ExtensionManifest[];
    capabilityOwners(capability: string): readonly string[];
    validate(document: Readonly<WorldDocument>): ExtensionValidationDiagnostic[];
    migrate(document: WorldDocument, fromVersion: string, toVersion: string): WorldDocument;
    snapshotHooks(): readonly import('./types.js').SnapshotExtensionHook[];
    schemaContributions(): readonly {
        id: string;
        root: Record<string, import('./types.js').JsonValue>;
    }[];
    resolve(requirements?: Readonly<Record<string, ExtensionRequirementDefinition>>, schemaVersion?: string): ExtensionResolutionDiagnostic[];
}
export declare function buildCompilerDependencyGraph(document: WorldDocument | NormalizedWorldDocument): CompilerDependencyGraph;
//# sourceMappingURL=architecture.d.ts.map