import type { CompiledCollider, Vec3 } from '../core/types.js';
import type { PlayerShape } from '../explore/CollisionWorld.js';
export interface TeleportResult {
    valid: boolean;
    position?: Vec3;
    colliderId?: string;
    reason?: string;
}
export declare class TeleportSystem {
    private colliders;
    private readonly shape;
    private readonly collisionWorld;
    constructor(colliders: readonly CompiledCollider[], shape: PlayerShape);
    setColliders(colliders: readonly CompiledCollider[]): void;
    test(origin: Vec3, direction: Vec3, maxDistance?: number): TeleportResult;
}
//# sourceMappingURL=TeleportSystem.d.ts.map