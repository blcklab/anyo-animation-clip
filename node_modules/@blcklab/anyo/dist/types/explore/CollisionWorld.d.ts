import type { CompiledCollider, Vec3 } from '../core/types.js';
export interface PlayerShape {
    height: number;
    eyeHeight: number;
    radius: number;
    stepHeight: number;
}
export interface MoveResult {
    position: Vec3;
    grounded: boolean;
    velocityY: number;
}
export declare class CollisionWorld {
    private colliders;
    constructor(colliders: readonly CompiledCollider[]);
    setColliders(colliders: readonly CompiledCollider[]): void;
    move(position: Vec3, delta: Vec3, velocityY: number, shape: PlayerShape): MoveResult;
    canOccupy(position: Vec3, shape: PlayerShape): boolean;
    findSupportAt(position: Vec3, maxHeight?: number): number | null;
    private moveAxis;
    private findBlocking;
    private findSupport;
}
//# sourceMappingURL=CollisionWorld.d.ts.map