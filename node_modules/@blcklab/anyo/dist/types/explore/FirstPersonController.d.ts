import type { ExplorationRuntimeController, PluginRuntimeContext } from '../core/types.js';
export interface FirstPersonControllerOptions {
    enabled?: boolean;
    inputEnabled?: boolean;
    /** Installs scoped browser keyboard, mouse, pointer-lock, and touch input. Defaults to true. */
    browserInput?: boolean;
    lookSensitivity?: number;
    touchLookSensitivity?: number;
    pointerLock?: boolean;
}
export declare class FirstPersonController implements ExplorationRuntimeController {
    private readonly context;
    private readonly options;
    private readonly keys;
    private readonly collisions;
    private readonly canvas;
    private readonly config;
    private velocityY;
    private yaw;
    private pitch;
    private disposed;
    private suspended;
    private enabledState;
    private inputEnabledState;
    private externalMove;
    private externalRun;
    private browserInputInstalled;
    private originalTabIndex;
    private readonly xrCleanups;
    private readonly touch;
    constructor(context: PluginRuntimeContext, options?: FirstPersonControllerOptions);
    get enabled(): boolean;
    get inputEnabled(): boolean;
    setEnabled(enabled: boolean): void;
    setInputEnabled(enabled: boolean): void;
    setMoveAxes(right: number, forward: number): void;
    setRun(running: boolean): void;
    addLookDelta(deltaX: number, deltaY: number): void;
    clearInput(): void;
    releasePointerLock(): void;
    setColliders(colliders: PluginRuntimeContext['compiled']['colliders']): void;
    update(deltaSeconds: number): void;
    dispose(): void;
    private canAcceptInput;
    private installBrowserInput;
    private uninstallBrowserInput;
    private readonly onKeyDown;
    private readonly onKeyUp;
    private readonly onBlur;
    private readonly onCanvasClick;
    private readonly onMouseMove;
    private applyLook;
    private readonly onTouchStart;
    private readonly onTouchMove;
    private readonly onTouchEnd;
    private getTouchMoveAxes;
    private detectRoom;
}
//# sourceMappingURL=FirstPersonController.d.ts.map