import type { CompiledEntityNode, CompiledPrimitive, CompiledWorld, ComponentQueryResult, WorldQueryLike } from '../core/types.js';
export declare class WorldQuery implements WorldQueryLike {
    private compiled;
    private readonly componentIndex;
    private readonly tagIndex;
    update(compiled: CompiledWorld | null): void;
    entity(id: string): CompiledEntityNode | null;
    entities(): readonly CompiledEntityNode[];
    components(type: string, options?: {
        enabledOnly?: boolean;
    }): readonly ComponentQueryResult[];
    tagged(tag: string): readonly CompiledEntityNode[];
    primitives(entityId: string): readonly CompiledPrimitive[];
}
//# sourceMappingURL=WorldQuery.d.ts.map