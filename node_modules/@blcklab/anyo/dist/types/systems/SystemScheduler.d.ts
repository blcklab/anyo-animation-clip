import type { PluginRuntimeContext, RuntimeSystemsOptions, SystemFrameDriver, WorldChange, WorldSystem } from '../core/types.js';
import type { RuntimeTransformStore } from './RuntimeTransformStore.js';
export interface SystemFrameResult {
    fixedSteps: number;
    interpolationAlpha: number;
}
export declare class SystemScheduler {
    private readonly transforms;
    private readonly warn;
    readonly systems: readonly WorldSystem[];
    readonly fixedDeltaSeconds: number;
    readonly maxSubSteps: number;
    readonly maxFrameDeltaSeconds: number;
    private readonly contexts;
    private baseContext;
    private accumulator;
    private frameNumber;
    private fixedStepNumber;
    private droppedTimeWarningEmitted;
    private active;
    constructor(systems: readonly WorldSystem[], options: RuntimeSystemsOptions | undefined, transforms: RuntimeTransformStore, warn: (message: string) => void);
    setup(baseContext: PluginRuntimeContext): Promise<void>;
    updateContext(baseContext: PluginRuntimeContext): void;
    beginFrame(deltaSeconds: number, time: number, driver: SystemFrameDriver): SystemFrameResult;
    lateFrame(deltaSeconds: number): void;
    applyChanges(changes: readonly WorldChange[]): Promise<void>;
    dispose(final?: boolean): void;
    get isActive(): boolean;
    private createContext;
    private setFrameState;
    private invoke;
}
//# sourceMappingURL=SystemScheduler.d.ts.map