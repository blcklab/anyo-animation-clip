import type { CompiledPrimitive } from '../core/types.js';
export interface ResolvedTextLayout {
    worldWidth: number;
    worldHeight: number;
    canvasWidth: number;
    canvasHeight: number;
    fontPixels: number;
    paddingPixels: number;
    lineHeightPixels?: number;
    align: 'left' | 'center' | 'right';
}
export declare function resolveTextLayout(primitive: CompiledPrimitive): ResolvedTextLayout;
//# sourceMappingURL=textLayout.d.ts.map