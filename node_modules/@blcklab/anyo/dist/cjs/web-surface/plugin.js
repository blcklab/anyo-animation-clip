"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.webSurfacePlugin = webSurfacePlugin;
const registry_js_1 = require("./registry.js");
const WebSurfaceRuntime_js_1 = require("./WebSurfaceRuntime.js");
function webSurfacePlugin(options = {}) {
    const registry = options.registry ?? (0, registry_js_1.createWebSurfaceAppRegistry)();
    let runtime = null;
    const capabilities = Object.freeze({
        registeredApps: true,
        snapshots: true,
        domOverlay: typeof document !== 'undefined',
        externalUrls: Boolean(options.externalUrls),
        liveXRPresentation: false,
    });
    return {
        name: 'anyo:web-surface',
        registry,
        capabilities,
        async setup(context) {
            runtime = new WebSurfaceRuntime_js_1.WebSurfaceRuntime({ ...options, registry });
            await runtime.sync(context);
        },
        update(_deltaSeconds, context) {
            runtime?.update(context);
        },
        async applyChanges(_changes, context) {
            await runtime?.sync(context);
        },
        dispose() {
            runtime?.dispose();
            runtime = null;
        },
    };
}
//# sourceMappingURL=plugin.js.map