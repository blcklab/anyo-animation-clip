"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.inspectWorldRuntimeSnapshot = inspectWorldRuntimeSnapshot;
exports.validateWorldRuntimeSnapshot = validateWorldRuntimeSnapshot;
exports.canonicalSnapshotString = canonicalSnapshotString;
const safePath_js_1 = require("../schema/safePath.js");
const canonical_js_1 = require("../document/canonical.js");
function inspectWorldRuntimeSnapshot(snapshot, options = {}) {
    const issues = [];
    try {
        (0, safePath_js_1.assertSafeObjectGraph)(snapshot, 'runtime snapshot');
    }
    catch (error) {
        issues.push({ severity: 'error', code: 'SNAPSHOT_UNSAFE', path: '', message: String(error instanceof Error ? error.message : error) });
    }
    if (snapshot.snapshotVersion !== '1.0')
        issues.push({ severity: 'error', code: 'SNAPSHOT_VERSION_UNSUPPORTED', path: '/snapshotVersion', message: `Unsupported snapshot version "${String(snapshot.snapshotVersion)}".` });
    if (!Number.isSafeInteger(snapshot.worldRevision) || snapshot.worldRevision < 0)
        issues.push({ severity: 'error', code: 'SNAPSHOT_REVISION_INVALID', path: '/worldRevision', message: 'worldRevision must be a non-negative safe integer.' });
    if (options.worldRevision !== undefined && snapshot.worldRevision > options.worldRevision && !options.allowFutureRevision)
        issues.push({ severity: 'error', code: 'SNAPSHOT_FUTURE_REVISION', path: '/worldRevision', message: `Snapshot revision ${snapshot.worldRevision} is newer than world revision ${options.worldRevision}.` });
    if (options.worldId && snapshot.worldId && snapshot.worldId !== options.worldId)
        issues.push({ severity: 'error', code: 'SNAPSHOT_WORLD_MISMATCH', path: '/worldId', message: `Snapshot world ${snapshot.worldId} does not match ${options.worldId}.` });
    const bytes = new TextEncoder().encode(JSON.stringify(snapshot)).byteLength;
    if (bytes > (options.maximumBytes ?? 16_000_000))
        issues.push({ severity: 'error', code: 'SNAPSHOT_SIZE_EXCEEDED', path: '', message: `Snapshot size ${bytes} exceeds the configured limit.` });
    return { valid: !issues.some((issue) => issue.severity === 'error'), issues, bytes };
}
function validateWorldRuntimeSnapshot(snapshot, options = {}) {
    const result = inspectWorldRuntimeSnapshot(snapshot, options);
    if (!result.valid)
        throw new Error(result.issues.map((issue) => `${issue.code} ${issue.path}: ${issue.message}`).join('\n'));
}
function canonicalSnapshotString(snapshot) {
    return JSON.stringify((0, canonical_js_1.canonicalizeJson)(snapshot));
}
//# sourceMappingURL=index.js.map