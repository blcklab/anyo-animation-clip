"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.xrExplorationPlugin = xrExplorationPlugin;
const XRExplorationController_js_1 = require("./XRExplorationController.js");
function xrExplorationPlugin(options = {}) {
    let controller = null;
    return {
        name: 'anyo:explore-xr',
        setup(context) {
            controller = new XRExplorationController_js_1.XRExplorationController(context, options);
        },
        update(deltaSeconds) {
            controller?.update(deltaSeconds);
        },
        applyChanges(changes, context) {
            if (changes.some(change => change.type === 'collider-state' || change.type === 'world-rebuild')) {
                controller?.setColliders(context.compiled.colliders);
            }
        },
        dispose() {
            controller?.dispose();
            controller = null;
        },
    };
}
//# sourceMappingURL=plugin.js.map